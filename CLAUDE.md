# CLAUDE.md

株式会社ヤエノワ コーポレートサイト（静的サイト / GitHub Pages）の編集ガイド。

> **本リポジトリは公開されている。** 機密情報（契約書本文・条番号・客先名・個人情報）を、
> サイト・リポジトリ・**コミットメッセージ**に書かないこと。

- コンテンツを編集する前に **「必須ルール」** を読む。
- **`outlook/` 配下を編集するときは `outlook/CLAUDE.md` も読む**（あちらに固有ルールがある）。

## 必須ルール（契約・法務）— コンテンツ編集時に厳守

1. **実績・事例の書き方** 🔴
   - 受託業務は秘密保持義務の対象。**特定案件を描写しない**。
   - 出さない要素: 客先名、**案件の説明における業界名**、規模を示す語（「大手」「大規模」等）、
     「複数ブランド」など案件を特定し得る表現。
   - 判断記録（所有者判断 2026-09-04）: 業界名を伏せるのは**案件を特定させない**ため。
     **代表の経歴として領域を述べること**（例: 金融・EC領域の業務システム開発の経験）は、
     **案件の具体（時期・システムの種別・規模・客先）と組み合わせない限り対象外**とする。
     組み合わせると一人法人では母数が小さく特定に近づくので、**経歴の記述に具体を足さない**。
     レビューで「業界名が出ている」という指摘が出たら、この行を根拠に据え置いてよい
     （ただし上の「組み合わせない」条件を満たしていることを確認したうえで）。
     **据え置きの対象は「代表の経歴として領域名を述べること」に限る。**
     役割の程度・年数・件数・規模の表現（「長年」「多数」「大手」等）や表現の強さに
     踏み込む指摘は対象外で、通常どおり検討する。撤回するときはこの行を消す。
   - 語り口は「過去に○○を担当した」ではなく **「○○に対応できる（提供領域・スキル）」**。
   - 例外: 当該パートナー企業から**書面による事前同意**を得た範囲のみ。
   - 判断記録（所有者判断 2026-08-22）: `/services/nippo` および `/services/nippo-slides` に載せる
     **自社ツールの画面キャプチャに写る項目名「案件（ブランド）」は、そのまま掲載する**。
     自社ツールのUIラベルであり案件の特定には当たらない、という所有者の判断。
     レビューで指摘が挙がってもこの判断を優先する（撤回するときはこの行を消す）。
2. **成果物を載せない** 🔴
   - 要件定義書・設計書・図・画面・コード等の**実物・抜粋・スクリーンショットは掲載不可**
     （著作権は委託元に帰属するため）。
3. **機密の取り扱い** 🔴
   - 契約書・NDA本文、条項、客先名をリポジトリにコミット／サイトに掲載しない。
4. **外部資料の引用** 🔴
   - **読んでいないURLを出典として貼らない。** Web検索が返すのは「リンク一覧」と
     「コーパス全体の要約」で、**要約の記述がどのリンクにあったかは保証されない**。
     両者を結びつけて出典として示すのは**出典の捏造**。
   - 一次資料を読めていない場合は、**数値を出さない**か「検索で得られた記述」と明示する
     （「未検証」の注記を添えて数値を載せるのは対策にならない。読者に届くのは数値のほう）。
   - **検索結果が矛盾したら、選ばずに報告する**（版の違いを疑う）。
   - 数値を扱うページは**書く前に一次資料を依頼する**。外部HTTPSが遮断され検索要約しか
     見られない実行環境がある（WebFetch が 403）。**PDF・スクリーンショットをもらえば
     全文検証できる**。
   - 数値には**出典・統計期間・シナリオ・参照日**を添える。古い資料は「推移は○年まで」と
     明記する（現在形は現況と誤読される）。
   - 引用は要約であっても**原典の語をなるべく保つ**。原典にない程度・継続性の修飾語
     （「大幅に」「〜し続けている」等）を足さない。
   - ※ 2026-08-01 に発生: 検索要約の数値を、読んでいない公的資料の出典つきで掲載した。
     当該資料に数値は存在しなかった。
5. **資格・認定** 🟡
   - 取得（合格）が確認できたものだけ掲載する。取得前・受験中の資格は載せない。
   - 表示＋構造化データ（`hasCredential`）をセットで用意する。

> 実務・構造面のリスク低減のための運用ルール。**法的拘束力のある最終判断は弁護士に確認**すること。

## 法務メモ

- **プライバシーポリシー**: `privacy.html`（`/privacy`）で対応済み。
  **アクセス解析やフォームを導入したら必ず改定**する。
- **特定商取引法**: 現状は不要。サイトはメール問い合わせのみ、配布しているアプリ・ツールは
  無償で、利用者から対価を得ていない。
  **オンライン申込・課金（アプリ内課金・有料版を含む）を始めたら表記ページの新設が必須**
  （outlook のデモを有料化する場合も同じ）。
- **景品表示法**: 誇大・誤認を招く表現を避け、**事実の裏付けを保持**する。
  能力表示（「対応できる」）にも合理的根拠が要る。裏付け資料は社内保持し、サイトには載せない。

### 所在地 🔴

- GMOオフィスサポートの**バーチャルオフィス（住所貸し）**。
  〒153-0064 東京都目黒区下目黒1丁目1番14号 コノトラビル7F。
  規約上Web掲載は可（標準的な「所在地」表記に留め、誇大・誤認表現を付けない）。
- **解約する場合、解約日までにサイト・名刺・登記等から当該住所を全て削除する義務がある**
  （GMO利用規約 第33条。怠ると違約金・自動更新）。
  **本リポジトリは公開されているため、サイトの表示だけでなくリポジトリ内の記述も削除対象。**
  削除箇所は **7箇所**: `about.html` の会社概要テーブル・`index.html` の JSON-LD の address・
  `llms.txt`・`privacy.html`・`audio-guide-privacy.html`・**この `CLAUDE.md` 自身**・
  `.claude/agents/site-consistency-check.md`。
  **住所を書く箇所を増やしたらこの一覧に追記すること**（`grep -rn "コノトラ" . --exclude-dir=.git` で確認できる）。
  同じ一覧を `scripts/check_site.py` の `ADDRESS_FILES` が写しとして持ち、**CI が増減を検出する**
  （スクリプトに住所そのものは書いていない。検索語は残っているファイルから実行時に組み立てる）。
  **正は本節の一覧**で、CI は一覧と実ファイルの食い違いを知らせるだけ。
  **一覧を直すときは CLAUDE.md と `ADDRESS_FILES` の両方**を直す。
- **解約するときの手順**（CI は住所を持たないため、全削除後は検査自体が成立しない）:
  0. **住所の語（郵便番号・町名丁目番地・建物名・区名）と、この手順そのものを、
     リポジトリ外のメモに控える。** 作業を進めると両方ともリポジトリ内から消える。
  1. 上記7箇所から住所を削除する。`index.html` は **`PostalAddress` ブロックごと**消す
     （`addressLocality`・`addressRegion` を残すと検索語に引っかからず素通りする）。
     **1箇所ずつ消す途中で CI は赤くなるが、赤を消すためにチェックを外さないこと**
     （残っている箇所を名指しで教えてくれる。そこが安全網の本体）。
  2. **画像・PDF・OGP画像などに住所が写り込んでいないかを目視で確認する。**
     テキストとして読めないファイルは CI も grep も検出しない。
  3. 🔴 **安全網を消す前に、人が確認する。** 控えた語を**それぞれ**
     `grep -rn <その語> . --exclude-dir=.git` にかけ、残るヒットが
     **本節・`scripts/check_site.py` の説明・`.claude/agents/site-consistency-check.md` の
     住所チェックの節だけ**であることを確かめる。**それ以外が1件でもあれば手順1に戻る。**
     （0件になるのは手順8のあと。**CI の「削除してよい」という出力を根拠にしないこと。**
     表記を変えて検索語を失っただけでも同じ出力になる。）
  4. 3 を通ってから、次を**まとめて削除**する:
     - `scripts/check_site.py` の `ADDRESS_FILES`・`address_tokens`・`collect_address_keys`・
       `check_address_locations`・**`CHECKS` への登録**（外し忘れると CI が NameError で落ちる）・
       **`EXPECTED_CHECKS` を 8 に下げる**・**`MIN_COVERAGE` の「住所」の行**・
       冒頭 docstring の住所への言及
     - `.claude/agents/site-consistency-check.md` の住所の値と住所チェックの節
     - CLAUDE.md のファイル表にある `scripts/check_site.py` の行の住所への言及
  5. 開発フローの「CI の9種」の列挙から住所の項目を外し、**同じ文の「9種」と
     ファイル表の「（9種）」を8種に直す**（`IMPROVEMENTS.md` の完了ログにも「9種」が
     あるが、**日付つきの当時の記録なので直さない**）。
  6. 🔴 **作業ツリーの0件は「表示面」の完了でしかない。** git 履歴には住所が残る
     （`git log -S <その語> --all` で確認できる）。force-push しても GitHub 上は
     到達不能コミットとして参照可能なまま残るため、**履歴からの除去はこの手順では完了しない**。
     本節冒頭が「リポジトリ内の記述も削除対象」と定めている以上、**どちらを採るかを先に決め、
     弁護士に確認する**: (a) 履歴は義務の範囲外とみなす（解釈を記録に残す）／
     (b) 範囲内とみなす（private 化・作り直し・GitHub Support への削除依頼）。
  7. リポジトリ外の掲載面も棚卸しする（登記・名刺・地図やビジネスプロフィール・
     過去の GitHub Actions の実行ログ・リポジトリの About など）。
  8. **最後に本節（所在地）を丸ごと削除し、そこで初めて全語の grep が0件になることを確認する。**
     ここまでの手順が本節の中にあるので、先に消すと残りが読めなくなる（控えたメモが頼り）。

## 開発フロー

- 作業ブランチで開発 → コミット → push → PR → マージ。
- **push が完了したら、指示を待たずに PR を作成してよい**（自動マージ運用）。
  **マージは CI（site checks）の完了と成功を確認してから。** 作成直後はまだ走っていない。
  コンテンツ変更時は下記レビューを通し、**🔴 が残っていない状態**でマージする。
  マージ後は PR の URL を報告する。
- コンテンツ変更後は必ずサブエージェントでレビューする（`.claude/agents/`）:
  - `content-legal-review` … 必須ルール（契約・法務）への違反チェック
  - `site-consistency-check` … JSON-LD 構文・4箇所同期・URL 正規化・sitemap・noopener
- **レビューの「事実」に関する指摘は、🔴 でなくてもマージのブロッカーとして扱う。**
  出典URLの妥当性・数値の裏付け・引用内容の一致に関する指摘は、解消するかユーザーの
  判断を仰ぐまでマージしない。「目視確認推奨」に格下げして公開しないこと
  （2026-08-01 に発生: 参照先ドメインの誤りの指摘を報告扱いにしてマージし、誤った出典が公開された）。
- コンテンツ変更時は実機（ブラウザ）で表示確認し、JSON-LD の構文を検証する。
- 機械的な整合性は CI（`.github/workflows/site-checks.yml`）が push・PR ごとに検査する。
  手元では `python3 scripts/check_site.py` で同じものを走らせられる（依存なし）。
  **CI が赤いままマージしない。** 検査内容は JSON-LD 構文・`noopener`・混在コンテンツ・
  住所の記載箇所・メールの表記ゆれ・apex 統一・内部リンクの実在と拡張子なしURL・
  sitemap の対応・`outlook/` の noindex の9種。
- 🔴 **PR を作ってすぐマージしない。チェックの完了を確認してからマージする**
  （作成直後は未完了なので、赤いはずのものが通ってしまう）。
- 🔴 **CI を黙って弱める変更をしない。** ワークフローの削除・`continue-on-error: true`・
  `if: false` の追加・`CHECKS` からの項目削除・**`CHECKS` の関数の中身を空にすること**・
  **`MIN_COVERAGE` を下げること**は、いずれも安全網の撤去にあたる。
  意図した増減なら `EXPECTED_CHECKS` と上の「9種」の記述も同時に直す
  （直さないと CI 自身が数の食い違いで落ちる）。
- **CI の通過はレビューの通過ではない。** 文言を見ないので `content-legal-review` の
  代わりにならず、**項目が重なる `site-consistency-check` の代わりにもならない**。
  次は CI に入っていない: 会社情報の**値の一致**（住所は「どのファイルにあるか」だけを見る）・
  資格と `hasCredential` の同期・FAQ 構造化データと表示内容の一致・
  sitemap に**載っていない**公開ページの検出。
- 🔴 **書くべきでない語を含むコミットは、push する前にメッセージを書き直す。**
  push 後に force-push で履歴から外しても、**GitHub は到達不能になったコミットを
  SHA 指定で参照できる状態に残す**（消すには GitHub Support への依頼が要る）。
  本文だけ直しても履歴には残る、という前提で書くこと。
- `solution-ideation` … 新規テーマの壁打ち用。レビュー担当ではないため通過必須ではないが、
  発想を公開文言にするときは content-legal-review を必ず通す。

## サイト全体の規約

**SEO / AIO**

- 正規URLは **apex ドメイン**（`https://yaenowa.co.jp/`、www無し）。
  canonical・OGP・sitemap・構造化データを統一する。
- コンテンツを変更したら **`llms.txt` と JSON-LD を同期**する（`llms.txt` 冒頭の「最終更新」も）。
- URLを増やしたら `sitemap.xml` と、必要に応じてナビ・FAQ構造化データを更新する
  （本文を変えたら lastmod も）。
- **FAQ 等の構造化データは、表示されている内容と一致させる**（Googleポリシー）。
- **`outlook/` 配下は noindex 運用で sitemap に載せない。** sitemap や `llms.txt` を
  横断的に見直すときも、この例外を崩さないこと（段階解除の方針は `outlook/CLAUDE.md`）。

**セキュリティ**

- 静的サイト（バックエンド・フォーム・認証なし）。攻撃面は小さい。
- 外部リンク（`target="_blank"`）には必ず `rel="noopener"` を付ける。
- 新しい外部リソース（スクリプト・フォント等）は提供元の信頼性を確認する。
- インラインJSにユーザー入力を流し込まない。

**アクセシビリティ**

- 装飾要素は `aria-hidden`。`prefers-reduced-motion` を尊重する。

## ファイル

| ファイル | 役割・注意 |
|---|---|
| `index.html` | トップページ。入口に徹する構成（ヒーロー／お悩み／対応領域／自社サービス／選ばれる理由／会社について要約／お問い合わせ）。外部フレームワーク不使用 |
| `business/` | 事業ページ。`/business/` が入口で、配下に4事業の個別ページ（requirements / introduction / ai / app）。トップの対応領域アコーディオンは要約で、詳細はこちら。`/business/` は検索流入の着地点として、**症状（いまの状態）から入口を選ぶ表**・4事業カード・**4つの関係（「決める」を土台に「移す」「効かせる」「作る」が乗る）**・相談の流れで構成する。症状の文言は各事業ページの「こんなときに」から採っているので、**個別ページを直したら一覧側の症状も見直す**。**4本柱を増減したら、トップの表示・JSON-LD の `hasOfferCatalog` と `/business/` の `hasPart`・症状の表・関係の図・`llms.txt`・sitemap をすべて揃える**。`business/ai` に「モデルの開発・ファインチューニング・AI製品の再販は行っていない」という**対応範囲の否定表現**があるため、その方針を変えるときはこの一文を必ず見直す（放置すると事実と異なる表示になる） |
| `about.html` | 会社案内。公開URLは拡張子なしの **`/about`**。社名の由来・代表紹介・会社概要・ビジョン。**代表の `Person` 構造化データはこのページにある**（表示と一致させるため） |
| `site.css` | `index.html` / `about.html` / `business/` 配下が共有するスタイル（ナビ・フッター・セクション共通・演出・レスポンシブ）。**読み込み順は theme.css → site.css → ページ固有の `<style>`** |
| `theme.css` / `theme.js` | 全ページ共通の基盤（カラーパレット・ダークモード・リセット・テーマ切替）。**色やテーマ挙動の変更はここだけを編集**し、各ページには書かない。theme.js は描画前に実行する必要があるため `<head>` で同期読み込み（**defer/async 禁止**） |
| `llms.txt` | AIO用の会社サマリ。**`index.html` と同期する** |
| `privacy.html` | プライバシーポリシー。公開URLは拡張子なしの **`/privacy`**（リンク・canonical・og:url・sitemap を統一）。改定時は `dateModified` と sitemap の lastmod を更新。**実態（取得方法・利用目的・外部サービスの利用状況）と一致させる** |
| `og-image.png` | SNSシェア用OGP画像。**24ページの `og:image` / `twitter:image` が参照する**（index / about / privacy / audio-guide-privacy / business / services / outlook 配下）。**キャッチコピーやデザインを変えたら再生成して差し替える**（JSON-LD の `logo` は実ロゴ `LOGO.png` のまま。OGPバナーとは別物）。**書き出しは 1200×630・パレット256色の PNG**（各ページが `og:image:width` に 1200 を宣言しているため、2倍サイズで書き出すと宣言と実物がずれる。フルカラーのままだと 200KB を超える） |
| `favicon.ico` / `favicon-32.png` / `apple-touch-icon.png` | ファビコン一式。**24ページが3行セットで参照する**（`icon`（ico・any）／`icon`（png・32x32）／`apple-touch-icon`）。素材は `LOGO.png` の**左上の花びら（「八」）だけを切り出したもの**（ロゴ全体を縮めると16pxで判読できないため）。**`favicon.ico` と `favicon-32.png` は背景を透過**（不透明にするとダークなタブバーで白い四角に見える）。**`apple-touch-icon.png` だけは不透明**にし、背景は `theme.css` の `--paper`（`#fdf8f6`）に合わせる（**iOS は透過部分を黒で埋める**ため）。ロゴを変えたら3つとも作り直す |
| `LOGO.png` | 実ロゴ（500×500）。**ナビの `<img>`（23ページ。ナビを持たない `services/nippo-slides.html` とリダイレクトスタブ2ページを除く全ページ）・トップのヒーローロゴ（`index.html` のみ）・`index.html` の JSON-LD の `logo` と `image`** が参照する。**ファビコンには使わない**（上記参照）。🔴 **差し替えるときは、書き出しツールのメタデータ（XMP・EXIF）を落としてからコミットする。** 2026-09-04 に、**書き出しツールのアカウント単位の識別子と代表の氏名**が埋まったまま公開されていたのを除去した（公開リポジトリなので、代表個人のアカウント単位の識別子を残さない方針。「技術コミュニティ CraftStage への関与」の判断記録と同じ趣旨。**具体の値・ツール名はリポジトリ外の記録に置く**）。確認は**チャンクを直接数える**（`IHDR` / `IDAT` / `IEND` 以外が出ないこと、`trailing 0` であること）: `python3 -c "import struct;d=open('LOGO.png','rb').read();i=8;exec(\"while i<len(d):\n n=struct.unpack('>I',d[i:i+4])[0];print(d[i+4:i+8].decode());i+=n+12\");print('trailing',len(d)-i)"` … Pillow の `Image.info` では**解釈できたチャンクしか出ない**ので、私的チャンクや `IEND` 以降の付加データを見落とす |
| `robots.txt` / `sitemap.xml` | SEO |
| `scripts/check_site.py` | 機械的な整合性チェック（9種）。CI から実行するが、手元でも `python3 scripts/check_site.py` で動く。**依存を増やさない**（標準ライブラリのみ）。住所そのものは書かない方針（検索語は住所が残っているファイルから実行時に組み立てる）。**grep の代替ではない** |
| `.github/workflows/site-checks.yml` | 上記を push・PR ごとに走らせる |
| `CNAME` | `yaenowa.co.jp`（apex） |
| `gx.html`（ルート直下） | 旧URL `/gx` からの meta refresh リダイレクトスタブ（noindex）。**編集・削除しないこと** |
| `outlook/` | 「今後の展望」セクション。→ **`outlook/CLAUDE.md`** |
| `IMPROVEMENTS.md` | 改善バックログ |

## 確認済み事項

- **事業範囲（所有者確認 2026-08-31）**: **アプリの受託開発も請ける方針**。
  自社プロダクト（`/services/nippo` の日報入力ツール、`/services/audio-guide` の
  歴史散歩音声ガイド）は、企画から実装・公開までを自ら通すために作ったもので、
  受託対応の裏付けとして位置づける。サイト上で「自社サービスに限る」と読める
  書き方はしない（対応領域の表示・JSON-LD の `hasOfferCatalog` と `knowsAbout`・
  `llms.txt` の3セクション）。

- **AIコンサルティングの根拠（所有者確認 2026-09-03）**: 導入支援・社内研修の実績は
  **守秘契約により対外開示できない**。ただし景表法の合理的根拠は**公開ではなく保持**が要件のため、
  サイトの記載は「対応します」の能力表示のまま維持してよい（表現を弱める必要はない）。
  **社内（非公開）には、実施日・形式・時間・扱った内容の記録を、客先を特定できる形のまま
  秘密保持の範囲で保管する**（根拠資料としての価値は特定性にあるため、社内記録では匿名化しない）。
  **保管先は本リポジトリの外**（クローン配下に置かない。作業ディレクトリに作ると公開経路に載る）。
  一方で**リポジトリ・サイト・コミットメッセージには客先を特定し得る情報を一切書かない**。
  公開できる裏づけは、技術コミュニティでの登壇など**外部から確認できる活動**で補う。
  ただしコミュニティ活動は「生成AIの知見の継続的な更新」の裏づけであり、
  **「社内研修に対応できる」の直接の根拠ではない**（サイトでも係り先を書き分ける）。
  レビューで**「実績を公開していない／根拠が示されていない」ことを理由とする指摘**が出たら、
  この行を根拠に据え置いてよい。**表現の強さ**（実績件数・効果の断定・比較優位の主張など）に
  踏み込む指摘は対象外で、通常どおり検討する。
  🔴 **この行に案件の属性（実施時期・業界・規模・件数）を書き足さないこと。**
  本リポジトリは公開されているため、書き足した時点で必須ルール1・3の違反になる。

- **技術コミュニティ CraftStage への関与（所有者確認 2026-09-04）**: 代表は
  **共同主催であり、かつ登壇者**。ただし connpass のページには共同主催である旨を記載して
  いないため、**「共同主催」の部分は第三者が外部から確認できない**
  （登壇は connpass のイベントページで確認できる。後述）。景表法の合理的根拠は
  公開ではなく保持が要件のため、「共同主催・登壇者」の記載は維持する。
  記載箇所は5つ（`business/ai.html` 本文・`about.html` 本文・`about.html` の Person の
  `memberOf.description` と `description`・`llms.txt`）と、`outlook/` の2箇所
  （`outlook/index.html`・`outlook/academics.html`）。**関与の書き方を変えるときは7箇所すべて揃える**。
  あわせて `index.html` の「選ばれる理由」と `llms.txt` の強み節にも、コミュニティ名を出さない
  「登壇・発信」への言及がある。**書き換えるときはこの2箇所も見直す**（一覧の7箇所には含めない）。
  **共同主催であることを示す社内記録**（運営上のやり取り・開催回の一覧・準備や運営の記録）を、
  **本リポジトリの外**に保管する。「公開ではなく保持が要件」という論拠は、保持の実体が
  なければ空振りする（資料の提出期限は短い。後から集める前提で構えないこと）。
  レビューで「共同主催が外部から確認できない」という指摘が出たら、この行を根拠に据え置いてよい。
  ただし**据え置きの対象は「CraftStage の共同主催という事実」に限る**。
  外部から確認できない事実一般の掲載を許す一般則ではなく、
  **役割の程度・頻度・規模の表現**（「主宰」「運営を統括」「毎月開催」等）や表現の強さに
  踏み込む指摘は対象外で、通常どおり検討する。
  なお `business/ai.html` の記述は「外部から確認できる裏づけ」として置いているが、
  **外部から確認できるのは登壇（LT）までで、共同主催は社内保持の根拠に依拠する**。
  登壇は connpass のイベントページで第三者が確認できる（所有者確認 2026-09-04）。
  **サイトからの外部リンクは、コミュニティのトップページへの既存4箇所にとどめる**
  （`about.html` 本文・`about.html` の Person の `memberOf.url`・`business/ai.html` 本文・`llms.txt`）。
  この4箇所は「外部から確認できる裏づけ」として**維持する**（減らす方向ではない）。
  🔴 **個別イベントページ・SNS への裏づけリンクを新たに足さないこと**（所有者判断 2026-09-04）。
  🔴 **代表個人のアカウント単位のリンク・`sameAs` を足さないこと。**
  代表個人のSNSにも登壇の発信があるが、**事業と切り離した個人アカウントのため載せない**
  （所有者判断 2026-09-04）。事業としての発信と個人としての発信を分けて扱うこと、
  `sameAs` は検索エンジンに会社の実体と結びつける指示になり引き返しにくいことによる。
  当該投稿は登壇の**社内根拠**として扱い、**プラットフォーム名・アカウント名・URLは
  リポジトリに書かない**
  （代表の実名は公開されているため、書くと実名と個人の発信が結びついた形で恒久的に残る）。
  この判断は CraftStage の登壇・共同主催に限った運用であり、**「外部から確認できる裏づけを
  示さなくてよい」という一般則ではない**。新しい主張を載せるときの根拠の要否は、
  通常どおり個別に判断する。
  `business/ai.html` の係り先は**「生成AIの知見の更新」に限定したまま**にする
  （「社内研修に対応できる」の直接の根拠にはしない）。
  connpass 側に共同主催が反映されれば、**「共同主催の根拠を社内保持に依拠させる扱い」と
  直前の係り先の限定**は見直せる。**個人SNS・`sameAs`・リンク箇所の上限はこれとは独立した
  判断で、見直しの対象に含めない。**

- **実績量の表現の裏づけ（所有者判断 2026-09-04）**: `about.html` の
  「Shopifyストア構築やコーポレートサイト制作を**数多く**手がけてきました」と
  `llms.txt` の「多数の構築経験」について、**件数の裏づけ資料は用意しない**という判断。
  景表法の合理的根拠は公開ではなく保持が要件だが、**保持もしない**という整理なので、
  表現を強める方向（件数・順位・比較優位の明示）へは進めないこと。
  レビューで「件数の裏づけ」が指摘されたら、この行を根拠に据え置いてよい。
  撤回するときはこの行を消す。

- **DNS（apex）**: `yaenowa.co.jp` の A レコードは GitHub Pages のIP（`185.199.108-111.153`）を
  正しく指しており、追加のDNS作業は不要。`www.yaenowa.co.jp` も github.io へ向いている。
  （変更時は GitHub の Settings → Pages の Custom domain = `yaenowa.co.jp` と Enforce HTTPS を確認）

## 自律改善ループ（週次）

週1回、Routine（スケジュール実行）が新しい Claude セッションを起動し、
監査 → 改善 → 検証 → 報告を自律的に実行する。バックログは `IMPROVEMENTS.md`。

**自律実行セッションでの無人マージの範囲**（人が指示した対話セッションには適用しない）:

- ✅ **技術系＝公開文言を変えない変更**（SEO・パフォーマンス・リファクタ・リンク修正・
  アクセシビリティ・CI 等）… レビューエージェント通過後、**CI の完了と成功を確認したうえで**
  自動マージしてよい。
  - ❌ ただし **`.github/workflows/` と `scripts/check_site.py` に触れる変更は、技術系でも
    無人マージの対象外**（安全網自身の変更なので人の承認を要する）
- ❌ **コンテンツ文言に触れる変更**（実績・資格・会社情報・キャッチコピー・法務文書等）
  … PR 作成と報告まで。**マージはユーザー承認後**（自律セッションでは絶対にマージしない）
  - 例外（所有者許可 2026-08-11）: `outlook/academics-guide-notes.html` への
    **エントリ追記のみ**の PR は自動マージしてよい（outlook/CLAUDE.md のページ別ルール参照）
- 判断に迷う変更はコンテンツ扱いにする（安全側に倒す）。
- 見た目を変えないはずの変更は Playwright 等でピクセル比較して確認する。
- 改善余地がない週は無理に変更せず「変更なし」で終了してよい。

## 既知の未対応事項（TODO）

- （現時点で対応待ちの大きな項目はなし）
